# Copy to .env and fill in real values. NEVER commit .env.
# docker compose reads .env automatically; deploy.sh sources it for its own checks.

# Telegram bot token from @BotFather
TELEGRAM_BOT_TOKEN=8720900550:AAGo3K6J_AxqPSMOLQwFlsqK_byivpkryCU

# Facebook App credentials (used by get_facebook_token.md flow)
FACEBOOK_APP_ID=1036434462501249
FACEBOOK_APP_SECRET=ecd6b7de2cd2643687a5e754a05170fc

# Single source of truth for the PostgreSQL password.
# postgres service and the app service both read this — they can never drift.
# On an EXISTING volume this must match the password baked in at first init.
# To change it on an existing deployment: docker compose down && docker volume rm <project>_pgdata
DB_PASSWORD=postgres
